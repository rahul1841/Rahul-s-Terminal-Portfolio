// Static username shown in the shell prompt: guest@portfolio:~$
export const USERNAME = "guest";

// Common Linux commands used to give a friendlier "not a real terminal"
// message when a visitor tries one of them.
export const LINUX_COMMANDS = [
  "ls", "cd", "pwd", "mkdir", "rm", "cp", "mv", "cat", "grep", "chmod",
  "chown", "sudo", "apt", "nano", "vim", "touch", "echo", "man", "ssh",
];
